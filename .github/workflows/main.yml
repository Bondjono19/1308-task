name: CI

on:
  push: 
    branches: main
  pull_request:
    branches: main
  workflow_dispatch: 
jobs:
  build:
    runs-on:  ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: run a one-line script
        run: echo Hello World!

      - name: multi line script
        run: |
          echo add other actions
          echo tu bild and test your porj.
